<h2 class="page-header">Edit Book</h2>
<form action="/books/<%= book.id %>?_method=PUT" method="POST">
    <%- include("_form_fields") %> 
    <a href="/books">Cancel</a> <%# uso un link anziché button perché i links sono usati per colegare cose in generale, i buttons per collegare forms%>
    <button type="submit">Update</button>
</form>