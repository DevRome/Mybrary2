<h2 class="page-header">Edit Author</h2>
<form action="/authors/<%= author.id %>?_method=PUT" method="POST">
    <%- include("_form_fields") %> 
    <div class="form-row form-row-end btn-row">
        <a class="btn btn-danger" href="/authors/<%= author.id %>">Cancel</a> <%# uso un link anziché button perché i links sono usati per colegare cose in generale, i buttons per collegare forms%>
        <button class="btn btn-primary" type="submit">Update</button>
    </div>
</form>